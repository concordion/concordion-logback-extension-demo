<configuration>
	<include resource="logback-include.xml"/>

	<root level="ALL">
		<!-- Normally level would be "STDOUT" so can see all logs in IDE, but for demo limiting to info as would on build server -->
		<appender-ref ref="STDOUT-INFO" />
		<appender-ref ref="FILE-PER-TEST" />
	</root>
</configuration>